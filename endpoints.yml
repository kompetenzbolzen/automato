host1:
  transports:
    ssh:
      class: transport.SshTransport
      hostname: 'localhost'
      username: 'jonas'
  commands:
    notify:
      class: command.NotifyCommand
      transport: ssh
  states:
    user:
      class: state.UserSessionState
      transport: ssh
      ttl: 30
